# Auth ChangeLog

## 2026-05-13

- 独立迁入登录、注册、登出与当前用户查询接口。
- 为新项目改用 `novel_generater_session` cookie，避免与旧平台会话串线。
- 前端独立迁入登录页、注册页与认证 store。
- 本地开发默认隐藏登录模块，小说工作台前端不再跳转登录页；后端小说接口无会话时使用本地默认创作者，可通过 `NOVEL_GENERATER_AUTH_DISABLED=0` 与 `VITE_NOVEL_GENERATER_AUTH_DISABLED=0` 恢复登录校验。
