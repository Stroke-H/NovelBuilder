# Runtime ChangeLog

## 2026-05-13

- 新建独立 `apps/web` 与 `apps/api` 目录结构。
- 新增脚本 `scripts/check.sh`。
- 新增独立 `.cursorrules`、项目级 skills 和本地配置约束。
- 参考旧项目 `start.sh` 优化本项目启动流程：新增根目录 `start.sh`，启动前检查前后端依赖、配置文件与端口占用，并将问题输出到本地日志；后端默认端口改为 `19081`，启动时按顺序等待后端和前端健康检查通过后再输出访问地址。
- 启动脚本增加端口兜底：默认端口被占用时自动选择冷门备用端口，并输出最终前后端访问地址。
- 启动脚本会将当前实际前后端地址写入 `.tmp/current-dev.env`，避免浏览器继续访问旧备用端口导致 Chrome 错误页。
- 收敛启动入口：删除 `scripts/dev-all.sh`、`scripts/dev-api.sh`、`scripts/dev-web.sh`，以后只使用根目录 `./start.sh` 启动前后端。

## 2026-05-15

- 新增全局后台任务悬浮球，可统一查看大纲生成、正文生成、审计、修订、文风模版提炼等运行中任务，并支持直接终止任务。
- 后端补充运行时任务注册、状态更新与取消能力，批量任务和长流程现在都可进入统一任务面板跟踪。
- 服务重启后会校正前端残留的“生成中”假状态，避免大纲或其他任务已中断却仍被界面误判为运行中。
- 优化小说项目加载查询，避免对大体积 `raw_json` 做数据库排序导致 `Out of sort memory`。
- 新增创作设置的本地持久化接口，支持管理 API Key 配置与文风模版，并对 `null` 配置返回做统一空数组兜底，避免设置弹窗渲染报错。
- 后台任务面板增加最近完成、失败与已终止状态展示；文风模版提炼失败时会保留错误原因，避免任务结束后只表现为“自动取消”。

## 2026-05-18

- 创作设置新增“常规设置”，支持配置小说章节数量最大值与单章节字数最大值。
- 新建小说、生成大纲与章节正文生成会读取常规设置上限，避免误填过大的章节数或单章目标字数。
- 常规设置扩展为高级参数面板：数据库连接、前端/AI 超时、上下文裁剪、大纲批处理、轮询、重试与后台任务保留时间均可查看和配置，并为每项补充性能瓶颈与安全范围说明。
- 前端请求超时、AI 请求超时、批量重试次数、大纲等待超时与轮询间隔会读取常规设置即时生效；数据库连接类参数在设置页标注为需重启后端并仍以启动配置优先。
