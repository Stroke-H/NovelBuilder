# NovelGenerater 开发者规则

本文件是 NovelGenerater 项目的顶级协作规则。所有 AI 助手、自动化 Agent 和人工开发者在修改本仓库时，都应优先遵循这些规则。

## 一、项目定位与技术架构

1. **项目定位固定**：本仓库是独立的小说智能生成应用，不再依赖 TestCenter 的平台布局、权限菜单或测试工具上下文。
2. **前端技术栈固定**：前端使用 Vue 3、TypeScript、Vite、Pinia、Vue Router、Element Plus。
3. **后端技术栈固定**：后端使用 Go、Gin、MySQL，并通过 HTTP API 为前端提供能力。
4. **目录约定固定**：
   - `apps/web`：独立前端
   - `apps/api`：独立后端
   - `.agents/skills`：项目级 skills
   - `docs/`：架构、迁移和流程文档
5. **小说生成能力必须完整保留**：大纲生成、后续章节续生成、正文生成、审计、修订、文风分析、素材提取等现有能力都属于核心链路，禁止为了“简化迁移”而降级。
6. **提示词与生成逻辑视为产品资产**：`open-novel-writing` skill、后端 prompt 常量、分批生成策略、JSON 解码与上下文裁剪逻辑，必须优先完整保留，再做结构优化。

## 二、执行工作流

1. **先理解再动手**：修改前先阅读相关文件和现有实现，不凭记忆猜测代码结构。
2. **迁移优先保真**：从旧项目迁入能力时，先保证行为、提示词、配置和接口语义一致，再考虑目录整理和命名优化。
3. **简单优先**：优先选择最小、直接、可验证的方案。不要为了展示能力而过度抽象。
4. **手术式修改**：每一行改动都应服务于当前用户需求。禁止顺手重构、顺手格式化、顺手删除无关代码。
5. **保留用户改动**：工作区可能包含用户或其他 Agent 的未提交修改。不要回滚、覆盖或清理自己没有确认来源的改动。
6. **高风险操作先暂停确认**：涉及历史重写、强推、批量删除、数据库结构调整、权限模型调整、数据迁移、真实密钥改写时，必须先列计划并等待用户确认。
7. **失败要继续定位**：遇到报错时，优先追踪根因，不要只做表面兜底。无法继续时要说明阻塞点和已验证的信息。

## 三、代码修改规范

1. **Vue 代码规范**：优先使用 Composition API、`<script setup>` 和 TypeScript。保持响应式数据来源清晰，避免不必要的全局状态。
2. **Pinia 规范**：登录态、当前用户、跨页面创作状态放入 Pinia；组件局部状态留在组件内。
3. **Go 代码规范**：保持 handler、service、model、database 分层。handler 负责参数和响应，service 负责业务逻辑，model 负责配置与提供方结构，database 负责连接管理。
4. **错误处理**：前后端都必须显式处理异常。前端要给出用户可理解的提示，后端要返回稳定的错误结构并记录必要日志。
5. **并发与覆盖保护**：涉及大纲后台续生成、正文生成、审计和修订的逻辑，必须防止后写覆盖先写，不能回退已有章节或覆盖已生成正文。
6. **命名规范**：变量和函数使用 `camelCase`，类型、组件、接口使用 `PascalCase`。文件和目录命名必须语义化。
7. **注释规范**：只在复杂业务、边界条件、并发流程、数据迁移、兼容逻辑处写解释性注释。不要写重复代码字面含义的废话注释。
8. **UI/UX 一致性**：优先延续小说工作流已有视觉语言。创作面板、素材图谱、章节流和项目卡片要统一、克制、清晰。
9. **不要硬编码旧平台路径**：不得在 NovelGenerater 中残留 `TestCenter` 平台专属路由、菜单、权限 key、平台 layout 或无关模块引用。
10. **运行地址约定**：开发态前端默认 `5174`，后端默认 `19081`。允许本地代理使用 `127.0.0.1`，但不要在用户可见文案里泄露旧项目地址。

## 四、配置与 Skills 规则

1. **真实配置本地保留**：`apps/api/data/ai_config.json`、`apps/api/data/database_config.json` 等真实配置文件可以本地存在，但不得提交到 GitHub。
2. **示例配置必须保留**：需要提交配置结构时，必须同步维护 `*.example.json`、README 或模板说明。
3. **AI Provider 选择逻辑不能私自改弱**：不能把现有 provider 选择、capability 回退顺序、输出 token 策略、上下文裁剪规则随意改成低质量兜底。
4. **项目级 skills 必须可追溯**：`.agents/skills` 中迁入的 skills 视为项目资产。修改 skill 时，要说明为什么改、影响哪个流程。
5. **Skill 与 Prompt 要保持一致**：如果后端提示词引用了 `open-novel-writing` 的方法论，skill 文档与 prompt 设计不能长期漂移。

## 五、验证闭环

1. **修改后必须自检**：代码修改完成后必须尽可能运行对应验证，再交付结果。
2. **前端改动验证**：涉及 Vue、Pinia、Router、样式或构建配置时，优先在 `apps/web` 下运行 `npm run build`。
3. **后端改动验证**：涉及 Go 代码、接口、服务、数据库逻辑时，优先在 `apps/api` 下运行 `go test ./...`。
4. **规则或文档改动验证**：纯文档、规则文件修改至少要读回检查内容，确认结构、语义和格式无误。
5. **无法验证必须说明**：如果因为依赖、网络、环境、数据库连接等原因无法运行验证，必须明确说明原因和风险。
6. **不要伪造验证结果**：没有实际执行的命令，不得声称已通过。

## 六、Git 与安全规则

1. **提交前检查暂存内容**：提交前必须确认 `git diff --cached` 中没有无关文件、运行产物、真实密钥或用户隐私数据。
2. **提交信息使用 Conventional Commits**：例如 `feat: migrate novel writer backend`、`fix: preserve generated chapters during outline batching`。
3. **单次提交聚焦**：一次提交只表达一个清晰主题。不要把功能迁移、密钥清理、格式化、临时调试混在一起。
4. **不要改写已发布历史**：除隐私清理、用户明确要求的历史整理外，不要 amend、rebase 或重写已推送提交。
5. **禁止提交真实敏感数据**：禁止提交 API Key、Token、Cookie、Session、真实邮箱、数据库密码和运行日志。

## 七、沟通与交付

1. **默认中文沟通**：面向用户的计划、解释、交付说明、风险提示默认使用中文。
2. **先结论后细节**：交付时优先说明做了什么、验证了什么、还有什么风险。
3. **不要制造额外工作**：能直接检查、修复、验证的事情不要交给用户；只有涉及产品取舍或高风险操作时才询问。
4. **明确假设**：如果必须基于假设推进，交付时要说明假设内容。
5. **保持上下文连续**：新会话或上下文不足时，应优先读取 `.cursorrules`、`.agents/skills/`、`docs/`、关键 router/store/service，避免重复踩旧坑。
6. **功能完成后必须执行收尾检查**：任何代码、配置、脚本、文档规则修改完成后，在对用户宣告“已完成”之前，必须先逐项执行一次 `Rule Closeout Checklist`，不得跳过。

### Rule Closeout Checklist

1. **检查是否需要更新文档或 ChangeLog**：涉及迁移、架构变更、用户可感知行为变化、重要 Bug 修复时，必须更新对应文档或记录文件。
2. **检查是否完成必要验证**：前端优先 `npm run build`，后端优先 `go test ./...`，纯规则或文档至少读回检查。
3. **检查是否存在高风险影响**：如果改动涉及登录态、数据库、AI 配置、真实密钥、数据迁移、强推，交付时必须明确说明影响范围和注意事项。
4. **检查工作区和提交范围**：准备提交时，必须确认暂存内容只包含本次目标相关文件，没有调试产物、敏感信息或无关改动。
5. **检查最终回复是否完整**：最终回复至少应说明本次做了什么、验证了什么、哪些项未验证、文档是否更新、是否已提交或推送。
